\documentclass{resume}
\usepackage[left=0.4 in,top=0.4in,right=0.4 in,bottom=0.4in]{geometry}
\newcommand{\tab}[1]{\hspace{.2667\textwidth}\rlap{#1}} 
\newcommand{\itab}[1]{\hspace{0em}\rlap{#1}}
{{#if title}}
\name{ {{title}} }
{{/if}}

{{#each information}}
\address{ {{#if text}} {{text}} {{/if}} }
{{/each}}
\begin{document}

{{#each sections}}
\begin{rSection}{ {{#if title}}{{title}}{{/if}} }
{{#each data}}
{\bf {{title}} } {{#if subtitle}}{{subtitle}}{{/if}} {{#if date}} \hfill { {{date}} } {{/if}} \\
{{#if text}}
{{text}}
{{/if}}
{{/each}}
\end{rSection}
{{/each}}

\end{document}
   